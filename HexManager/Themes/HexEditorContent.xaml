<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:HexManager">

    <Style x:Key="{x:Type local:HexEditorContent}" TargetType="{x:Type local:HexEditorContent}">
        <Setter Property="Background"
                Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"/>
        <Setter Property="Foreground"
                Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
        <Setter Property="BorderBrush" Value="#FF688CAF" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Stylus.IsFlicksEnabled"
                Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:HexEditorContent}">
                    <Border 
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True"
                        Padding="{TemplateBinding Padding}">

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="Hex Values" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            <StackPanel Grid.Row="1" Orientation="Horizontal">
                                <local:HeaderCell Text="00"/>
                                <local:HeaderCell Text="01"/>
                                <local:HeaderCell Text="02"/>
                                <local:HeaderCell Text="03"/>
                                <local:HeaderCell Text="04"/>
                                <local:HeaderCell Text="05"/>
                                <local:HeaderCell Text="06"/>
                                <local:HeaderCell Text="07"/>
                                <local:HeaderCell Text="08"/>
                                <local:HeaderCell Text="09"/>
                                <local:HeaderCell Text="0A"/>
                                <local:HeaderCell Text="0B"/>
                                <local:HeaderCell Text="0C"/>
                                <local:HeaderCell Text="0D"/>
                                <local:HeaderCell Text="0E"/>
                                <local:HeaderCell Text="0F"/>
                            </StackPanel>
                            <StackPanel Grid.Row="2" Orientation="Vertical"/>
                        </Grid>

                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>