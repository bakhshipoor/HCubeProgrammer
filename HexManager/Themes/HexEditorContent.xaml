<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:HexManager">
    
    <BooleanToVisibilityConverter x:Key="BoolToVis" />

    <Style x:Key="{x:Type local:HexEditorContent}" TargetType="{x:Type local:HexEditorContent}">
        <Setter Property="Background"
                Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"/>
        <Setter Property="Foreground"
                Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
        <Setter Property="BorderBrush" Value="#FF688CAF" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="1 0 0 0"/>
        <Setter Property="Stylus.IsFlicksEnabled"
                Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:HexEditorContent}">
                    <Border 
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True"
                        Padding="{TemplateBinding Padding}">
                        
                        <!--<StackPanel x:Name="PART_Contents" Orientation="Vertical"/>-->
                        
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <Border 
                                Grid.Row="0" 
                                BorderThickness="0 0 2 2" 
                                BorderBrush="Gray" 
                                Margin="-2 0 1 0"
                                Visibility="{Binding TypeHeadearVisibility,RelativeSource={RelativeSource Mode=TemplatedParent},Converter={StaticResource BoolToVis}}">

                                <TextBlock 
                                    x:Name="PART_TypeHeader" 
                                    Text="{Binding TypeHeaderText,RelativeSource={RelativeSource Mode=TemplatedParent}}" 
                                    VerticalAlignment="Center" 
                                    HorizontalAlignment="Center"
                                    Height="30"/>
                                
                            </Border>
                            
                            <local:HexEditorContentDataHeaderColumns 
                                x:Name="PART_DataHeader"
                                Grid.Row="1" 
                                Height="30"
                                ParentHexEditorContent="{Binding RelativeSource={RelativeSource AncestorType=local:HexEditorContent}}" 
                                Visibility="{Binding DataHeaderVisibility,RelativeSource={RelativeSource Mode=TemplatedParent},Converter={StaticResource BoolToVis}}"/>
                            
                            <local:HexEditorContentData 
                                x:Name="PART_ContentData"
                                Grid.Row="2" 
                                ParentHexEditorContent="{Binding RelativeSource={RelativeSource AncestorType=local:HexEditorContent}}"/>
                            
                        </Grid>

                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>